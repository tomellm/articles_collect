[package]
name = "web_app"
version = "0.1.0"
edition = "2024"

[dependencies]
web_app_lib = { workspace = true }

tokio = { workspace = true }


[features]
hydrate = [
    "web_app_lib/hydrate"
]
ssr = [
    "web_app_lib/ssr"
]
